{
  "name": "hardhat-project",
  "license": "MIT",
  "version": "0.0.0",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "generate-verification": "node scripts/generate-verification-files.js",
    "build-full": "npm run build && npm run generate-verification"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-network-helpers": "^1.0.10",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "hardhat": "^2.22.5"
  }
}
